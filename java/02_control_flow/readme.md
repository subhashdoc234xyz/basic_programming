# Control Flow

In this folder, you will learn how to control the execution path of your Java code.

## Concepts covered:
- **Conditionals**: `if`, `else if`, `else`, and the `switch` statement.
- **Loops**: `for`, `while`, and `do-while` loops.
- **Break and Continue**: Controlling loop termination and iteration skip.